import descriptionOf from "./descriptionOf";
import imageOf from "./imageOf";
import stackSizeOf from "./stackSizeOf";

const inventoryGetters = { descriptionOf, imageOf, stackSizeOf };

export = inventoryGetters;
